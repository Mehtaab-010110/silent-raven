"use client";

import React from 'react';

const GifWorkSection = () => {
  return (
    <section className="h-screen bg-black">
      {/* Empty - ready for GIF content */}
    </section>
  );
};

export default GifWorkSection;